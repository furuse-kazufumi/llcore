# 文脈を 4 倍にしたら Transformer のメモリは 5 倍に膨れ、recurrent は 1mm も動かなかった — 長文脈ほど「構造」で勝つ

> **本連載の全 llcore 実測は、自宅 CPU の極小モデル PoC であり、大手の実 LLM(12B〜64B)の性能を直接反証・凌駕するものではない。比較は手法・思想・計測規律の次元で行う。本記事の数値は実機の peak working set(占有メモリの最大値)の実測だが、対象は極小モデル(n_embd 256 / 4 層)で、絶対値ではなく「文脈長に対する増え方のトレンド」を読む。**

連載「自宅 CPU から見た 2026 年 6 月の LLM 業界」アーキテクチャ編。前回([a7](a7-control-theory-ssm.md))は「制御理論はとっくに『定数状態で過去を運ぶ』を解いていた」という話でした。今回はその**実機メモリでの帰結**です。

別の記事([b2](b2-suppress-your-win.md))で、私は長文脈の検索性能を測ろうとして **自宅マシン(RAM 3.6GB)のメモリが先に音を上げた**話を書きました。2048 トークンの full-attention forward が working set 3.9GB に膨れてスワップした。あれは事故のように見えて、実は**構造が予告していた必然**です。この記事は、その必然を数字で見せます。

結論を先に出します。

> **文脈長を 1024 → 4096 トークン(4 倍)にしたとき、Transformer(GPT)の peak メモリは 331.8MB → 1673.0MB と約 5.04 倍に膨れた。同じ条件で recurrent は 205.4MB → 205.6MB、つまり 1mm も動かなかった。**

---

## 0. 何を測ったか(30 秒)

軽量モデルを 3 アーキテクチャ用意し、文脈長(一度に食わせるトークン数)を 1024 / 2048 / 4096 と変えながら、**別プロセスに隔離して peak working set(WinAPI の `PeakWorkingSetSize`)を実測**しました。

- **GPT**: 標準の Transformer。注意機構(attention)で全トークンが全トークンを見る。
- **recurrent**: 定数状態 RNN 型。過去を固定サイズの「状態」に畳んで運ぶ。
- **RWKV**: 線形注意系。これも定数状態で過去を運ぶ設計。

用語を 1 つだけほどきます。

- **peak working set(WS)**: プロセスが実行中に物理メモリ上に持った量の最大値。「このプログラムを動かすのに、最悪どれだけ RAM が要るか」の実測値。OS が見ている本物の数字で、理論式ではありません。

---

## 1. 数字の全公開

`scripts/recurrent_runtime_rss.py --lengths 128,256,512,1024,2048,4096`(実機 2026-06、極小モデル n_embd 256 / 4 層 / 8 head)の実測。単点でなく **32 倍レンジの曲線**で見ます:

| 文脈長 L | GPT peak WS | recurrent peak WS | RWKV peak WS |
|---:|---:|---:|---:|
| 128 | 222.3 MB | 205.5 MB | 215.4 MB |
| 256 | 230.7 MB | 205.2 MB | 215.7 MB |
| 512 | 247.7 MB | 205.3 MB | 216.1 MB |
| 1024 | 331.8 MB | 205.4 MB | 216.0 MB |
| 2048 | 607.9 MB | 205.6 MB | 216.0 MB |
| 4096 | **1673.0 MB** | **205.6 MB** | 215.9 MB |
| **128→4096(×32)の倍率** | **×7.53** | **×1.00** | **×1.00** |

**曲線の形が一番の証拠です。** GPT の膨張率は計測レンジに依存します:128→512(×4)では **×1.11** とほぼ平坦——この領域は固定重み(モデル本体)が peak を支配し、attention の O(L²) 項はまだ誤差に埋もれます。ところが 512→4096(×8)では **×6.75**——ここで二次項が固定床を追い越し、曲線が跳ね上がります。「文脈 ×N でメモリ ×M」という単一の見出しは、**どこから測ったかで M が激変する**(同じ装置で ×1.11 にも ×6.75 にもなる)。だから単点でなく曲線で出します。recurrent / RWKV は全域で 205 / 216 MB に貼り付き(×1.00)、定数状態が文脈長と無関係であることを 32 倍レンジで裏取りしました。

> 補足: 1024 と 2048 の点は別日の独立ラン(`out/recurrent_runtime_rss_long.json`)と 0.1 MB 差で一致し、クロスラン再現性も確認済み。

![文脈長 128→4096(対数軸)で GPT の peak WS が、短文脈(128→512)では 222→248MB とほぼ平坦なのに長文脈で 608→1673MB へ急騰する一方、recurrent/RWKV は 205MB で平坦という折れ線グラフ](../../../assets/articles/llcore_context_memory.svg)

*図:横軸は対数。GPT(赤)は短文脈(128→512)で固定重み床に隠れてほぼ平坦、長文脈で二次項が顔を出し急騰(全レンジ 128→4096 で ×7.53)。recurrent/RWKV(緑)は定数状態で全域平坦(×1.00)。T=4096 で GPT は recurrent の 8.1×。極小モデルの実測で、読むのは絶対値でなく増え方の形。(公開時は SVG を raw 絶対 URL で参照すること)*

読みどころは 3 つです。

1. **GPT は超線形に膨れる。** 文脈 4 倍(1024→4096)で **メモリ 5.04 倍**、全レンジ 32 倍(128→4096)では **7.53 倍**。比例を超えています。これは attention のスコア行列が L×L、つまり **O(L²)** で増えるため。しかも伸び方は一定でなく、**短い文脈ほど固定重みに隠れて平坦(128→512 で ×1.11)、長くなるほど二次項が顔を出して鋭くなる(512→4096 で ×6.75)**。「文脈 ×N でメモリ ×M」の M は計測レンジ次第——だから単点でなく曲線で読む。
2. **recurrent と RWKV は動かない。** 文脈 4 倍でも **×1.00**。過去を固定サイズの状態に畳むので、文脈長に依存する項が原理的に無い。
3. **T=4096 で GPT 1673MB は recurrent 205MB の 8.1 倍。** そして外挿すると **T=8192 で GPT は約 6.5GB** —— 私の自宅マシン(3.6GB RAM)では**物理的に載らない壁**。一方 recurrent は 205MB のまま、びくともしない。

[b2](b2-suppress-your-win.md) で私のマシンがスワップした 3.9GB は、まさにこの曲線の上の点でした。事故ではなく、**O(L²) の予告どおり**だったわけです。

---

## 2. なぜこうなるのか(構造の話)

GPT の attention は「全トークンが全トークンを見る」総当たりです。トークンが L 個あれば、見る組み合わせは L×L。だからメモリも計算も **O(L²)** で膨らむ。さらに推論を速くするための KV キャッシュ(過去のキー/値を貯める)は **O(L)** でメモリを食い続けます。どちらも「文脈が伸びれば伸びるほど増える」項です。

recurrent / RWKV は発想が逆です。過去を 1 つずつ読みながら**固定サイズの状態ベクトルに畳み込み**、それを次へ運ぶ。状態の大きさは文脈長に依存しません(中身は数 KB の定数)。だから何トークン読もうと、メモリの「伸びる項」が存在しない。

これは [a7](a7-control-theory-ssm.md) で書いた「制御理論の状態空間モデル」そのもの。理論が「定数状態で過去を運べる」と言っていたことが、実機の peak WS で**平坦な直線**として現れた、という話です。

---

## 3. 隠さない但し書き(この数字の限界)

honest disclosure を連載の核に据えている以上、この測定の限界を最初に開示します。

- **極小・未学習モデルです。** n_embd 256 / 4 層のランダム初期化モデル。メモリ挙動はアーキテクチャで決まり学習状態に依存しない(ので計測には妥当)ですが、**絶対値**を大手 LLM にそのまま当てはめないでください。読むべきは**増え方のトレンド**です。
- **peak WS は「素の torch ランタイム + 固定の重み + 文脈依存バッファ」の合算です。** recurrent の「平坦 205MB」の大半は torch のベースライン(別記事 #59 で測った支配項)で、定数状態そのものは KB オーダー。つまり 205MB は「定数状態が重い」のではなく「**伸びる項が無い**」ことを示しています。クリーンな信号は絶対値でなく **GPT は膨張・recurrent は平坦という対比**です。
- **GPT は実運用では block_size で文脈を切り詰める**ので、ここまで素直には膨れません。本測定は「厳密に長文脈を保持するなら最低どれだけ要るか」の必要量を見たものです。

---

## 4. 先行研究の位置づけ(車輪の再発明ではない)

attention が O(L²)・KV キャッシュが O(L) でメモリを食うこと、SSM/線形注意(Mamba・RWKV 等)が定数状態で長文脈を平坦に扱えることは、**確立した知見**です([a7](a7-control-theory-ssm.md) / SUPRA / Mamba 系)。本記事の新規性はそこではありません。

差別化軸は **「北極星=working set を小さく予測可能に」という運用目標に照らして、commodity な 3.6GB マシンの実機 peak RSS で『どちらが構造的に勝つか』を測り切った」**点にあります。量子化(静的にビットを削る)は #53 で 2bit が報われなかったように頭打ちがある。本命は**動的支配項=文脈長で伸びる項を、そもそも持たないアーキテクチャを選ぶこと**。「bit を下げる」ではなく「**構造を変える**」——それを感想でなく実測曲線で示しました。

---

## 5. 教訓 — 「速さ」より「予測可能性」

長文脈の時代に効いてくるのは、ピーク性能より **「メモリが文脈長に対してどう振る舞うか予測できること」** です。GPT は速いし強い。でも文脈が伸びると、メモリが O(L²) で**予測不能な崖**に向かう。自宅 3.6GB マシンは T=8192 で崖から落ちる。recurrent は遅くても、**何トークン来ても 205MB で平らなまま**——予測できる。

> 量子化は「今あるものを薄く削る」。
> アーキテクチャ選択は「伸びる項を最初から持たない」。
> 長文脈ほど、後者が構造で前者を引き離す。

私のマシンが [b2](b2-suppress-your-win.md) で長文脈に殺されたのは、この曲線の上で起きた必然でした。次に同じ計算を回すときは、定数状態側に寄せる——それが、自宅 CPU から見えた一番実用的な結論です。

---

_技術注記:`scripts/recurrent_runtime_rss.py --lengths 128,256,512,1024,2048,4096`(別プロセス隔離 + WinAPI peak WS)。32 倍曲線の統合データは `out/recurrent_runtime_rss_curve32.json`(128–2048 は `recurrent_runtime_rss_curve.json`、4096 は別ランの `recurrent_runtime_rss_long.json` から統合、重複点 1024/2048 は 0.1 MB 差で一致)。T=4096 は attention 行列が ~2.1GB 相当で RAM 3.6GB 機では実走が swap 律速になるため当該点のみ別ランの値を採用。`RecurrentLM/RWKVLM.streaming_nll`(純粋追加)は block_size 制限なし・O(chunk) メモリで任意長文脈をスコアでき、GPT が attention O(L²) + block_size 制限で届かない領域を recurrent は平坦に処理する。回帰テスト `tests/unit/test_recurrent_streaming.py` 6 件 + 既存回帰 green。対象は極小ランダムモデルにつき絶対値でなくトレンドを読むこと。_
