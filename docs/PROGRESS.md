# PROGRESS — セッション再開ポインタ

このファイルは **Stop hook 非管理** の再開ポインタ。**再開フローの正本は `next_plan.md`** であり、本ファイルは再開地点の要約に限定する。

## 再開手順

1. まず [next_plan.md](next_plan.md) を読む
2. 特に「このセッションの最終到達点」と「次の具体的な一手」から再開する
3. `SESSION_SUMMARY.md` は hook に上書きされうるため、進捗の正本として扱わない

## 現在の要点

- Kaggle push handoff の **進捗状態の正本は `docs/next_plan.md`**。historical push candidate `D:\projects\llcore_kaggle_livecheck_20260617c` は `Kernel version 1 successfully pushed` まで到達したが、その後 `kaggle kernels status furusekazufumi/llcore-lm-compare` は `KernelWorkerStatus.ERROR` を返し、取得済み log `D:\projects\llcore_kaggle_output_20260617\llcore-lm-compare.log` には `ModuleNotFoundError: No module named 'llcore'` が残った。`D:\projects\llcore_kaggle_livecheck_20260617d` は script-kernel co-located 依存の停止済み候補、`...20260617e` は初代 dataset-backed 候補、`...20260617f` は `kaggle datasets create` 自体は成功したものの `src/` / `llcore/` が未アップロードで **manifest ↔ 実体が乖離した partial publish** を踏んだ historical 候補として保持する。現行の正本候補は archive-backed dataset-backed な `D:\projects\llcore_kaggle_livecheck_20260617g` で、`prepare_kaggle_lm_compare_bundle.py --dataset-source furusekazufumi/llcore-lm-compare-support --run-runner` 成功、`kaggle_push_readiness.py rc=0`、関連局所 gate は `95 passed`。`dataset_payload/` は `config.json` / `input_corpus.txt` / `src_llcore.zip` / `pkg_llcore.zip` / `LICENSE` / `NOTICE` / `dataset-metadata.json` / `dataset_payload_manifest.json` を持ち、runner は archive sha256 を検証してから安全に展開して `llcore` を import する。Kaggle 側 `furusekazufumi/llcore-lm-compare-support` は Python API `dataset_status(...) == "ready"` まで確認済みなので、次の外部操作は原則 `kaggle datasets version -p D:\projects\llcore_kaggle_livecheck_20260617g\dataset_payload --dir-mode zip -m "update dataset payload"` の人間判断である。必要なら create に戻せるが、その場合は visibility (`private` / `public`) を明示確定する。preflight には `dataset_payload/` と zip member の secret/path scan も追加済みで、`OPENAI_API_KEY` / `KAGGLE_KEY` / `KAGGLE_API_TOKEN` / private key marker / `D:\projects\...` のような local path があれば publish 前に fail-closed で止める。進捗確認の正本は `docs/next_plan.md` と Kaggle Web UI (`https://www.kaggle.com/datasets/furusekazufumi/llcore-lm-compare-support`) を使う。
- **現在の主作業ブランチは `feat/lm-recurrent`**。LM recurrent 実験の正本は `docs/LM_RECURRENT_PLAN.md` と tracked artifact `docs/artifacts/lm_recurrent_*` で、再開フロー自体の正本は引き続き `next_plan.md`
- LM recurrent 比較は **verdict packet 完成** まで到達:
  - `docs/artifacts/lm_recurrent_verdict.md` に PPL 表 / memory@T 曲線 / caveat を集約
  - strongest claim は「**RWKV が最も再現性の高い候補**」。`64/160` の 3 seed と `64/240` の 3 seed で raw PPL 最良と unigram floor 通過を維持
  - ただし full winner は未宣言。GPT と Recurrent の相対順位は seed-sensitive のまま
- artifact/verdict 回帰保護は実装済み:
  - `tests/unit/test_lm_artifacts.py` が tracked JSON から verdict row / `6/6` claim / md / svg を再計算して照合
  - 正式 gate は `py -3.11 -m pytest tests/unit -k lm -q && py -3.11 -m mypy src/llcore/lm/ && py -3.11 -m ruff check src/llcore/lm/`
- LM recurrent の duplicate SVG 整理は **canonical 化まで完了済み**:
  - `13bcc26` で `lm_recurrent_pilot160.svg` を canonical として残し、byte 同一だった block_size=64 SVG 6 枚を削除
  - `f6c7bb2` で canonical 化後の doc 記述を現状へ整合
  - LM recurrent 側の承認待ちはこの件については解消済み
- 現在の人間ゲート待ちは別ストリームが中心:
  - `verified_safe_learning` staging の publish 判断
  - `self_evolving_agents` staging の publish / rerun 関連判断

- `verified_safe_learning` staging は完了、人間ゲート待ち
- `self_evolving_agents` staging も完了、人間ゲート待ち
- `self_evolving_agents` は `INDEX.md` 件数補正と `/` リンク修正まで反映済み
- `corpus2skill` 側では stopword / resume 問題の再発防止修正まで完了
- `--resume-summaries` の positive round-trip はテストで確認済み
- marker 無し legacy summary の互換再読込、empty vocabulary fallback、0-score 語借用防止まで反映済み
- 比較用 rerun `self_evolving_agents_corpus_v2_stopwordcheck` により top-level label は改善確認、ただし off-topic 混入は残存
- `queries_refined_candidate.txt` を staging に追加し、`fetch_arxiv_topical.py` は今後 `Source Query` を paper markdown へ保存する
- 2026-06-15 時点で `queries_refined_candidate.txt` はさらに精密化済み。ただし **効果は未検証** で、`ti:` 化により recall が落ちる可能性があるため、本実行前に「改善候補」として扱う
- `Source Query` は本文メタ行だと TF-IDF 汚染になるため、source-query comment + loader 側メタ除去へ修正済み
- temp fetch 検証では loader 後 top terms / `_make_label()` に `ti` `cat` `source` `query` は残らず、query 汚染遮断を確認
- `runner._strip_skill_header()` は H1 限定へ補正し、`## Overview` 始まりの legacy summary も resume 対象に戻した
- `scripts/p1_compare.py` は、旧 `_CFG_RE` の未配線な死にコードを廃止し、`_CFG_BLOCK_RE` + フィールド別正規表現による legacy `cfg={...}` fallback を新規に配線済み。`tests/unit/test_p1_compare.py` で新形式 arch 行 / 旧 cfg 行 / eval-only / `dropout=0.0` / `[model]\t` 接頭辞 / partial cfg / model 行なし cfg / `_markdown_table` の `params=None` guard、optional セルの `None`→`?` 正規化、`ppl_gate_pass` 欠落時の tri-state 表示、headline の ratio 欠落時スキップと `--json` 非巻き添え、required field より前にネスト cfg が来た時の `arch n/a` graceful degradation を固定し、`py -3.11 -m pytest tests/unit/test_p1_compare.py -q` は 12 passed。`py -3.11 -m ruff check scripts/p1_compare.py tests/unit/test_p1_compare.py` と `py -3.11 -m mypy scripts/p1_compare.py` も pass
- `scripts/p1_compare.py` はその後さらに補強し、run log よりも **`model.pt` の checkpoint config を arch 正本として優先**するようにした。これにより `lm_aozora_drop` の stale legacy log (`dropout=0.0`) でも table は `drop0.1` を表示し、`tests/unit/test_p1_compare.py` は 13 passed に更新。現時点の `py -3.11 scripts/p1_compare.py` は `lm_aozora_realp1` (`ctx256`, `drop0.2`, `max_iters=1000`) を正しく拾い、`ratio_model_over_unigram=0.1782` が smoke-best `lm_aozora_drop` (`0.1504`) を上回れず **held-out 未改善** と報告する。
- `scripts/p1_compare.py` はさらに harden し、`model.pt` 読み込み失敗でも集計全体を落とさず log/verdict 側へ graceful fallback するようにした。checkpoint 側 `config: None` 由来で arch 表示に literal `"None"` が出る経路も潰し、表には **`extra corpora` 件数**を追加、headline でも `extras(base=..., p1=...)` を出すようにした。`tests/unit/test_p1_compare.py` は unreadable checkpoint fallback と extra-corpus count 表示まで固定し、現時点で 15+ source に対する型/静的解析も通過。
- `scripts/memory_footprint_harness.py` は memory@T の実測補助に加えて **Windows の commit/pagefile snapshot** (`system_before` / `system_after`) を JSON へ残すようにした。`system_before` は **3 モデル構築後・計測 loop 前**の headroom を表す。ここでの `avail_commit` / process commit は **速度指標ではなく OOM 回避 headroom の観測**であり、pagefile 調整は「速くする」ためではなく長時間 run の commit-limit kill を避けるための判断材料として扱う。
- その後の hardening として、system telemetry と process telemetry は **部分成功を許容**するように分離した。つまり `GetProcessMemoryInfo()` が失敗しても `avail_phys` / `avail_commit` は `system_before/after` に残り、process 側だけ `None` へ落ちる。さらに `--lengths` は **正の整数のみを fail-closed で受理**し、空要素 (`64,,128`) や非整数 (`64,abc`) も reject する。JSON には raw 入力の `config.lengths` と、昇順・重複排除した `lengths_effective` の両方を残し、後者を実効値として読む。
- 続けて harness に `--warmup`（既定 1）を追加し、先頭点の lazy allocation ノイズを下げた。RSS Δ は引き続き補助指標で、負値（ページアウト）は 0 に丸める。加えて recurrent 側も state を live のまま `after` を測るようにして、GPT logits と **測定点を対称化**した。warmup の CLI 伝播も回帰で固定した。なお Windows telemetry の ctypes struct / WinAPI 呼び出し自体は fake 置換で shape 回帰を見ており、**実 struct layout の妥当性は live Windows 実行で確認済み**。最新の検証は `py -3.11 -m pytest tests/unit/test_memory_footprint_harness.py -q` = `5 passed`、`py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare or p1_manifest_reconcile or memory_footprint" -q` = `212 passed, 401 deselected`、`py -3.11 -m mypy scripts/memory_footprint_harness.py tests/unit/test_memory_footprint_harness.py`、`py -3.11 -m ruff check scripts/memory_footprint_harness.py tests/unit/test_memory_footprint_harness.py` が通過。
- 2026-06-17 追記: `_parse_lengths()` は `sorted(set(...))` をやめ、**入力順保持 + dedup** に変更した。これで `lengths_effective` と headline の `T first→last` がユーザー指定順を保つ。局所回帰 `tests/unit/test_memory_footprint_harness.py` では `128,64,128 -> [128, 64]` と dedicated order test を固定済み。
- `src/llcore/lm/trainer.py` / `src/llcore/lm/__main__.py` の **char-LM train checkpoint/resume** は、その後の統合レビュー指示も反映して harden 済み。`Trainer.state_dict()` / `load_state_dict()` は optimizer・`batch_gen`/`eval_gen` に加えて **global torch RNG**・history・best_val・iter を保存し、CLI `train` は各 eval ごとに `<out>/train_state.pt` を自動更新する。snapshot の `train_meta` には `corpus` / `corpus_file` / `config` に加え **`val_frac` と corpus SHA256** を保存し、resume 時は split 不一致や corpus 内容 drift を fail-closed で弾く。`--max-iters` は resume 時のみ延長を許可し、`--batch-size` / `--eval-iters` / `--seed` の変更指定は snapshot 値を優先して明示 warn する。`tests/unit/test_lm_trainer.py` では continuous 6 step と 3+3 step resume の一致を **dropout=0.0 と 0.2 の両方**で固定し、`tests/unit/test_lm_cli.py` では snapshot artifact 生成、CLI resume の実動作、`val_frac=0.2` 維持、corpus drift fail-closed を回帰保護した。LM gate は `py -3.11 -m pytest tests/unit -k lm -q` = `96 passed, 413 deselected`、`py -3.11 -m mypy src/llcore/lm/`、`py -3.11 -m ruff check src/llcore/lm/ tests/unit/test_lm_trainer.py tests/unit/test_lm_cli.py` が通過。なお **既存の** `out/lm_aozora_realp1_run.log` は feature 追加前の途中ログなので、このファイル単体からは resume できず、今後の rerun から `train_state.pt` が得られる。
- `src/llcore/lm/__main__.py` にはさらに **データ追加導線**として `--extra-corpus-file` を追加した。base corpus (`--corpus` または `--corpus-file`) の後ろへ UTF-8 corpus を順序付きで連結でき、snapshot `train_meta` にも `extra_corpus_files` を保存して resume 時に照合する。`out/corpus_aozora.txt` 単独入力では `_load_corpus()` 前後の len と SHA256 が一致し、単一路径の改行正規化差は比較に混入していないことも確認した。加えて `eval` は OOV 文字数/率を verdict (`oov_chars`, `oov_rate`) に記録し、OOV>0 なら `stderr` に warning を出す。resume では `--dropout` 変更指定も無視 warn 対象へ追加し、`iter_num>=max_iters` の完了 checkpoint は「`--max-iters` で延長要」と明示終了する。`tests/unit/test_lm_cli.py` では追加 corpus の tokenizer 反映と verdict 記録、eval OOV warning まで固定し、LM gate は最新で `py -3.11 -m pytest tests/unit -k lm -q` = `98 passed, 416 deselected`、`py -3.11 -m mypy src/llcore/lm/`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_compare.py tests/unit/test_lm_cli.py tests/unit/test_lm_trainer.py tests/unit/test_p1_compare.py` が通過。これで P1 の「データ追加」は CLI 契約として受けられ、比較表にも visible になった。
- train snapshot 書き込みはさらに harden し、`_save_training_snapshot()` を temp file + `os.replace()` の **atomic save** に変更した。これで eval 途中の `train_state.pt` 直上書きによる partial-write リスクを避けつつ、`.tmp` を残さないことも `tests/unit/test_lm_cli.py` で固定した。resume 決定性テストは `tests/unit/test_lm_trainer.py` の helper 化に合わせて **`eval_interval=2` の sparse-eval ケース**まで追加し、`eval_interval=1` 専用の保証に縮まないよう補強した。LM gate は更新後に `py -3.11 -m pytest tests/unit -k lm -q` = `100 passed, 416 deselected`、`py -3.11 -m mypy src/llcore/lm/`、`py -3.11 -m ruff check src/llcore/lm/ tests/unit/test_lm_cli.py tests/unit/test_lm_trainer.py` が通過。
- 追加統合指示も反映し、snapshot temp 名は固定 `train_state.pt.tmp` から **`train_state.pt.<pid>.<counter>.tmp`** へ変更した。これで同一 process / 同一 `--out` でも cleanup が他方の tmp を消しにくくなる。`_save_training_snapshot()` には「**rename atomicity のみを狙い、power-loss durability は対象外**（fsync 未実施）」のコメントも明記した。回帰は `tests/unit/test_lm_cli.py` に failure-path を追加し、`os.replace` 失敗時でも既存 `train_state.pt` が無傷で tmp が残らないこと、さらに **CLI 経路そのもの**で `max_iters=16` による sparse-eval resume が continuous run と完全一致することを固定した。最新 gate は `py -3.11 -m pytest tests/unit -k lm -q` = `102 passed, 416 deselected`、`py -3.11 -m mypy src/llcore/lm/`、`py -3.11 -m ruff check src/llcore/lm/ tests/unit/test_lm_cli.py tests/unit/test_lm_trainer.py` が通過。
- P1 の次の重い rerun 前の cheap inspection として、[scripts/p1_corpus_probe.py](D:/projects/llcore/scripts/p1_corpus_probe.py) を追加した。これは `llcore.lm train` と**同じ改行正規化**で base corpus + `--extra-corpus-file` 候補を読み、各 extra について chars / vocab / SHA256 / base tokenizer に対する **new char 数と OOV 率** を即時集計する。`out/corpus_aozora.txt` 単体では chars `320730`・vocab `3044`・SHA256 `58ed1634a988...` を再確認し、既存 P1 記録と整合した。回帰は [tests/unit/test_p1_corpus_probe.py](D:/projects/llcore/tests/unit/test_p1_corpus_probe.py) の 3 case で、正規化・OOV 集計・CLI JSON 出力を固定し、`py -3.11 -m pytest tests/unit/test_p1_corpus_probe.py -q` / `py -3.11 -m mypy scripts/p1_corpus_probe.py` / `py -3.11 -m ruff check scripts/p1_corpus_probe.py tests/unit/test_p1_corpus_probe.py` が通過。
- 追加統合指示に合わせ、正規化/連結ロジックは [src/llcore/lm/corpus.py](D:/projects/llcore/src/llcore/lm/corpus.py) へ **torch-free の pure helper** として切り出した。`__main__._load_corpus()` と `scripts/p1_corpus_probe.py` の base / extra / combined は同じ `read_utf8_corpus_file()` / `load_utf8_corpus_files()` を通るため、「train と同じ正規化」がコード上も単一実装になった。probe 側の preview は 12 文字超過時に `...(+N more)` を出し、表ヘッダも `new chars vs base (uniq)` と `OOV vs base (occurs)` へ補正、extra `sha256` が**単体候補ファイルの指紋**であることも note に明示した。加えて script 起動経路は repo `src/` を自前で解決し、[tests/unit/test_p1_corpus_probe.py](D:/projects/llcore/tests/unit/test_p1_corpus_probe.py) には `subprocess` で `py -3.11 scripts/p1_corpus_probe.py ...` を実際に起動する smoke 回帰を追加した。最新 gate は `py -3.11 -m pytest tests/unit/test_p1_corpus_probe.py -q` = `5 passed`、`py -3.11 -m pytest tests/unit -k lm -q` = `102 passed, 421 deselected`、`py -3.11 -m mypy src/llcore/lm/`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py tests/unit/test_p1_corpus_probe.py tests/unit/test_lm_cli.py tests/unit/test_lm_trainer.py` が通過。
- その次の ergonomics として、extra corpus の束を **manifest** で扱えるようにした。`src/llcore/lm/corpus.py` に `read_corpus_manifest()` / `resolve_extra_corpus_files()` を追加し、blank 行と `#` comment を飛ばしつつ manifest 相対 path を解決する。CLI `train` / `eval` と `scripts/p1_corpus_probe.py` は `--extra-corpus-manifest` を受け、従来の `--extra-corpus-file` と ordered に合成する。これで次の P1 rerun は長い file 列挙を CLI に直書きせず、候補束を 1 ファイルに固定して probe → train → eval を回せる。回帰は [tests/unit/test_lm_corpus.py](D:/projects/llcore/tests/unit/test_lm_corpus.py)、[tests/unit/test_lm_cli.py](D:/projects/llcore/tests/unit/test_lm_cli.py)、[tests/unit/test_p1_corpus_probe.py](D:/projects/llcore/tests/unit/test_p1_corpus_probe.py) に追加し、manifest 相対解決、train/eval の snapshot/verdict 記録、probe の manifest 読み込みを固定した。最新 gate は `py -3.11 -m pytest tests/unit -k lm -q` = `106 passed, 422 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py tests/unit/test_lm_corpus.py tests/unit/test_p1_corpus_probe.py tests/unit/test_lm_cli.py` が通過。`py -3.11 scripts/p1_corpus_probe.py out/corpus_aozora.txt --json out/p1_corpus_probe_base.json` も chars `320730` / vocab `3044` / SHA256 `58ed1634a988...` を再確認した。
- 統合レビューの follow-up として、manifest 解決をさらに fail-closed に寄せた。`resolve_extra_corpus_files()` は explicit / manifest の両方を **`.resolve()` で対称化**し、順序保存の dedup を入れ、base corpus と同一 path の extra は除外する。`read_corpus_manifest()` は欠落 path を後段へ流さず、**manifest path + 行番号 + 元の記述**を含む `FileNotFoundError` で即 fail-closed する。`#` comment は full-line 先頭のみ対応で、inline comment 非対応である旨を help / docstring に明記した。回帰は [tests/unit/test_lm_corpus.py](D:/projects/llcore/tests/unit/test_lm_corpus.py) に explicit/manifest の対称解決、dedup + base 除外、欠落行の行番号付きエラーを追加して固定した。
- あわせて検証証跡も修正した。`py -3.11 -m pytest tests/unit -k lm -q` では `test_p1_corpus_probe.py` / `test_p1_compare.py` を collect しないため、report 用 gate は **`py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare" -q`** に差し替えた。最新の実測は `129 passed, 401 deselected` で、probe / compare / corpus を含む。併せて `py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py` と `py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py tests/unit/test_lm_corpus.py tests/unit/test_p1_corpus_probe.py tests/unit/test_lm_cli.py tests/unit/test_lm_trainer.py tests/unit/test_p1_compare.py` が通過した。
- cheap triage の実運用をさらに詰め、`scripts/p1_corpus_probe.py` は **filtered manifest の書き出し**まで担うようにした。`--write-manifest <path>` と `--max-oov-rate` / `--max-new-chars` を渡すと、probe で観測した extras から条件を満たす path だけを manifest 化し、そのまま `train` / `eval` へ流せる。これで「候補を見て手で転記する」手順を消せる。回帰は [tests/unit/test_p1_corpus_probe.py](D:/projects/llcore/tests/unit/test_p1_corpus_probe.py) に追加し、filter 選別と manifest 書き出しを固定した。最新 gate は `py -3.11 -m pytest tests/unit/test_p1_corpus_probe.py -q` = `8 passed`、`py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare" -q` = `131 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py`、`py -3.11 -m ruff check scripts/p1_corpus_probe.py tests/unit/test_p1_corpus_probe.py` が通過。
- 統合レビューの follow-up として、probe の監査性をさらに補強した。`_selected_extra_paths()` は丸め後 `oov_rate_vs_base` ではなく **生値 `oov_rate_vs_base_raw`** で閾値比較するため、境界値の誤選別を避ける。`--max-oov-rate` / `--max-new-chars` で選別が走った場合は、全候補ベースの `combined` に加えて **selected subset の `combined_selected`** を再計算し、CLI 出力では `[selected subset]` 表として、JSON では `combined_selected` キーとして残す。これで「表示した bundle」と「実際に train/eval へ流す manifest」の乖離を潰した。
- `--write-manifest` の出力 path は、可能な限り **manifest 出力先の親ディレクトリ基準の相対 path** で書き、跨ドライブ等で相対化不能な場合だけ絶対 path にフォールバックするようにした。probe が書いた `# Generated by ...` ヘッダ付き manifest を `train` がそのまま消費できる round-trip も [tests/unit/test_lm_cli.py](D:/projects/llcore/tests/unit/test_lm_cli.py) に固定した。`resolve_extra_corpus_files()` の Windows 大小文字差はまだ raw string 等価ベースなので、将来 mixed-case duplicate が実害化したら case-fold key 比較へ進む旨をコメントで残してある。
- 最新 gate は `py -3.11 -m pytest tests/unit/test_p1_corpus_probe.py tests/unit/test_lm_cli.py -q` = `21 passed`、report 用 gate `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare" -q` = `134 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py tests/unit/test_p1_corpus_probe.py tests/unit/test_lm_cli.py` が通過。
- Aozora extra corpus の準備自体も 1 手で済むよう、[scripts/p1_prepare_aozora.py](D:/projects/llcore/scripts/p1_prepare_aozora.py) を追加した。zip URL 群を CLI 直指定または `--url-manifest` で受け、`clean_aozora()` と同じ cleaning を通した UTF-8 corpus 群へ展開し、必要なら `--write-manifest` で probe/train/eval 直結の corpus manifest まで書く。出力名は `aozora_<cardid>_<zipstem>.txt` を自動導出し、別 URL が同名へ衝突する場合は fail-closed で止める。
- 共有ロジックとして [src/llcore/lm/data.py](D:/projects/llcore/src/llcore/lm/data.py) に `extract_aozora_text_from_zip_bytes()` を切り出し、既存 `fetch_aozora_text()` も同 helper 経由へ整理した。これで Aozora zip の unzip + clean 契約は script 側と train 側で重複しない。
- 回帰は [tests/unit/test_p1_prepare_aozora.py](D:/projects/llcore/tests/unit/test_p1_prepare_aozora.py) と [tests/unit/test_lm_data.py](D:/projects/llcore/tests/unit/test_lm_data.py) に追加し、cleaned UTF-8 出力、URL manifest 読み込み、relative corpus manifest 書き出し、output 名衝突 fail-closed、subprocess 起動、zip 抽出 helper を固定した。最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `140 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py scripts/p1_prepare_aozora.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py scripts/p1_prepare_aozora.py tests/unit/test_lm_data.py tests/unit/test_lm_corpus.py tests/unit/test_lm_cli.py tests/unit/test_lm_trainer.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_compare.py tests/unit/test_p1_prepare_aozora.py` が通過。
- その後の統合レビュー反映として、`p1_prepare_aozora.py` の fail-closed を **実行内 collision だけでなく既存ファイル衝突まで**拡張した。`prepare_aozora_corpora()` は download/write 前に全 URL の出力名を pre-flight し、(a) 異 URL 同士の同名衝突、(b) `out_dir` 既存ファイルとの衝突、を先に検出して中断するため、rerun で静かに既存 corpus を上書きしない。加えて URL は `https` + `aozora.gr.jp` / `www.aozora.gr.jp` の allowlist に制限し、zip 展開や cp932 decode が落ちた場合は failing URL を含む例外へ包んで束運用時の診断性を上げた。cp932 前提は維持しており、**非 cp932 zip は 1 件で束全体が fail-closed** する。
- prepare script の回帰は同じ [tests/unit/test_p1_prepare_aozora.py](D:/projects/llcore/tests/unit/test_p1_prepare_aozora.py) を拡張し、pre-flight collision、既存ファイル overwrite 拒否、URL allowlist、failing URL 文脈まで固定した。gate 数自体は変わらず、`py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `140 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py scripts/p1_prepare_aozora.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py scripts/p1_prepare_aozora.py tests/unit/test_lm_data.py tests/unit/test_lm_corpus.py tests/unit/test_lm_cli.py tests/unit/test_lm_trainer.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_compare.py tests/unit/test_p1_prepare_aozora.py` が引き続き通過。
- さらに rerun 摩擦を下げるため、prepare script は **per-corpus provenance sidecar** (`*.txt.source.json`) を書くようにした。sidecar には URL / path / chars / vocab / sha256 / `reused_existing` を記録し、同一 URL で rerun した場合はこの metadata が揃っている既存 corpus を **download せず再利用**する。一方で metadata 欠落や URL 不一致の既存ファイルは引き続き fail-closed で拒否する。これで「同じ URL のやり直し」は idempotent に軽くしつつ、別 URL への静かな取り違えは許さない。
- この provenance reuse まで含めた最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `146 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py scripts/p1_prepare_aozora.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py scripts/p1_prepare_aozora.py tests/unit/test_lm_data.py tests/unit/test_lm_corpus.py tests/unit/test_lm_cli.py tests/unit/test_lm_trainer.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_compare.py tests/unit/test_p1_prepare_aozora.py` が通過。
- 統合レビューの follow-up として、reuse 経路の検証も URL 一致だけで終わらせないよう締め直した。`_plan_output_paths()` は **URL allowlist (`https` + `aozora.gr.jp` / `www.aozora.gr.jp`) を pre-flight** で先に通し、reuse / non-reuse のどちらでも download 前に不正 host を拒否する。reuse 実行時は sidecar `sha256` と実ファイルの再計算 `sha256` を照合し、不一致なら fail-closed で再利用を拒否する。sidecar JSON が object でない場合も `AttributeError` ではなく `ValueError` で clean に拒否するよう揃えた。なお **redirect 先 host の再検証**はまだ未対応で、これは次の hardening 候補として残る。
- これを固定するため [tests/unit/test_p1_prepare_aozora.py](D:/projects/llcore/tests/unit/test_p1_prepare_aozora.py) は、placeholder `sha256` と `example.com` fixture を廃止し、same-URL reuse の正しい `sha256` fixture、ファイル改変後の reuse 拒否、batch 中の非 Aozora host を download 前に拒否するケース、非 Aozora URL sidecar の reuse 拒否、non-object metadata の clean reject を追加した。最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `150 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py scripts/p1_prepare_aozora.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py scripts/p1_prepare_aozora.py tests/unit/test_lm_data.py tests/unit/test_lm_corpus.py tests/unit/test_lm_cli.py tests/unit/test_lm_trainer.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_compare.py tests/unit/test_p1_prepare_aozora.py` が通過。
- その残件だった redirect 追従も塞いだ。`scripts/p1_prepare_aozora.py` は `urllib.request.build_opener()` に **no-redirect handler** を差し込み、Aozora fetch 中の 3xx を `HTTPError` として fail-closed に落とす。これにより pre-flight で allowlist を通った初期 URL から、runtime redirect で別 host へ逃げる経路は閉じた。回帰は [tests/unit/test_p1_prepare_aozora.py](D:/projects/llcore/tests/unit/test_p1_prepare_aozora.py) に redirect 拒否 case を追加し、最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `151 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py scripts/p1_prepare_aozora.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py scripts/p1_prepare_aozora.py tests/unit/test_lm_data.py tests/unit/test_lm_corpus.py tests/unit/test_lm_cli.py tests/unit/test_lm_trainer.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_compare.py tests/unit/test_p1_prepare_aozora.py` が通過。
- 追加統合レビューの follow-up で、prepare script の **test 健全性と batch atomicity** も揃えた。`tests/unit/test_p1_prepare_aozora.py` は production 経路どおり `urllib.request.build_opener()` を差し替える fake opener へ統一し、死んでいた `urlopen` monkeypatch を除去した。redirect 回帰も opener ごと置き換えるだけの疑似 test ではなく、実 `_NoRedirectHandler` を通す形へ変更したため、`redirects are not allowed` 文言と handler 配線の両方を固定する。
- あわせて `scripts/p1_prepare_aozora.py` の download 段は **batch-atomic** に変更した。各 URL の fetch/clean 結果は一旦メモリに貯め、全件成功した場合のみ corpus 本体と sidecar metadata をまとめて書き出す。これで後続 URL の decode/redirect/zip 失敗時に先行 corpus だけ disk に残る状態は解消した。`test_prepare_aozora_corpora_is_batch_atomic_on_late_download_failure` がこの回帰を固定する。
- `cast(Message[str, str], ...)` 由来の runtime `TypeError` も除去し、redirect 時の失敗は意図どおり `HTTPError("redirects are not allowed ...")` を `ValueError` へ包む経路に揃えた。最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `152 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py scripts/p1_prepare_aozora.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_compare.py scripts/p1_prepare_aozora.py tests/unit/test_lm_data.py tests/unit/test_lm_corpus.py tests/unit/test_lm_cli.py tests/unit/test_lm_trainer.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_compare.py tests/unit/test_p1_prepare_aozora.py` が通過。
- さらに write 段の overclaim も潰した。`scripts/p1_prepare_aozora.py` は corpus/sidecar を **tmp へ stage → `os.replace()` commit → 失敗時 cleanup** するように変え、`output` と `sidecar` の片肺だけ残る orphan を防ぎつつ、後続 file の commit failure 時もこの batch が作った corpus 群を巻き戻す。これで prepare は fetch/clean だけでなく **prepared output の commit まで batch-atomic** と言える形になった。
- ついでに CLI も整え、`--write-manifest` と `--json` は親ディレクトリを自動作成し、URL 0 件は生 `ValueError` ではなく `argparse` の usage + exit 2 へ寄せた。回帰は `tests/unit/test_p1_prepare_aozora.py` に second-corpus commit failure、sidecar commit failure、nested manifest/json path、empty URL argparse error を追加して固定した。最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `155 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_prepare_aozora.py`、`py -3.11 -m ruff check scripts/p1_prepare_aozora.py tests/unit/test_p1_prepare_aozora.py` が通過。
- その次の監査層として、manifest 本体の横に **bundle provenance JSON** を自動生成するようにした。`src/llcore/lm/corpus.py` に ordered corpus file 群の `path / chars / vocab / sha256` と normalized combined corpus の `sha256` をまとめる `build_utf8_corpus_bundle()` を追加し、`scripts/p1_prepare_aozora.py --write-manifest ...` は `<manifest>.bundle.json` を、`scripts/p1_corpus_probe.py --write-manifest ...` も同様の sidecar を書く。これで「manifest の path 列」だけでなく、その時点で実際に指していた corpus 内容と順序を 1 ファイルで固定できる。
- probe 側の出力 path も prepare に揃えて整理した。`--json` / `--write-manifest` は親ディレクトリを自動作成し、manifest の相対 path は Windows でも `/` 区切りへ正規化される。回帰は [tests/unit/test_lm_corpus.py](D:/projects/llcore/tests/unit/test_lm_corpus.py)、[tests/unit/test_p1_corpus_probe.py](D:/projects/llcore/tests/unit/test_p1_corpus_probe.py)、[tests/unit/test_p1_prepare_aozora.py](D:/projects/llcore/tests/unit/test_p1_prepare_aozora.py) に追加し、bundle JSON 生成と nested 出力を固定した。最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `156 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py tests/unit/test_lm_corpus.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_prepare_aozora.py` が通過。
- 追加統合レビューの follow-up として、probe の **空選択**を壊さないよう締めた。`--write-manifest` で全 extras が filter 落ちしても header-only manifest を正常に書き、`<manifest>.bundle.json` は生成せず `[selection] 0 extras selected; skipped manifest bundle metadata.` を明示する。`--json` の `combined_selected` も base-only の combined へ直したため、write-manifest 無しでも静かに「全部入り」を返す誤値は消えた。
- bundle 契約も honest 側へ寄せた。`build_utf8_corpus_bundle()` は `base_file=` を受けられるようにし、probe manifest の bundle は **base+selected extras の train 順**で `combined.sha256` を計算する。一方 prepare manifest の bundle は extras-only のままなので、payload には `combined.includes_base` を残して意味を明示する。さらに `bundle_sha256` は path 混入をやめ、ordered file `sha256` 列だけの fingerprint に変更した。これでマシン/OS 配置差による不要な不一致を避けられる。
- `_sha256_text` の三重定義も解消し、prepare / probe は `llcore.lm.corpus.sha256_text()` を共有するよう整理した。回帰は [tests/unit/test_lm_corpus.py](D:/projects/llcore/tests/unit/test_lm_corpus.py) に base 含有 bundle と path 非依存 fingerprint を、[tests/unit/test_p1_corpus_probe.py](D:/projects/llcore/tests/unit/test_p1_corpus_probe.py) に empty selection + write-manifest/json を、[tests/unit/test_p1_prepare_aozora.py](D:/projects/llcore/tests/unit/test_p1_prepare_aozora.py) に `includes_base=false` を追加して固定した。最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `158 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py tests/unit/test_lm_corpus.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_prepare_aozora.py` が通過。
- その次の層として、manifest **消費側**も sibling bundle を検証するようにした。`src/llcore/lm/corpus.py` に `verify_corpus_manifest_bundle()` を追加し、`resolve_extra_corpus_files()` は manifest 読込時に `<manifest>.bundle.json` が存在すれば `manifest_sha256` と再計算した bundle payload を照合する。これにより train / eval / probe 再実行のどれでも、manifest 本体や base 側の drift があれば fail-closed で止まる。
- 回帰は [tests/unit/test_lm_corpus.py](D:/projects/llcore/tests/unit/test_lm_corpus.py) に matching bundle accept と manifest/base drift reject を、[tests/unit/test_lm_cli.py](D:/projects/llcore/tests/unit/test_lm_cli.py) に probe が書いた manifest を train が consume する際の drift reject を追加した。最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `161 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py tests/unit/test_lm_corpus.py tests/unit/test_lm_cli.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_prepare_aozora.py` が通過。
- 追加統合レビューの blocker だった **prepare bundle mismatch** も解消した。verify は bundle 自身の `combined.includes_base` 契約に従って `base_file` を使う/使わないを決め、比較から `files[].path` を外したので、prepare の extras-only bundle と probe の base+extras bundle が同じ消費層を通る。これで absolute path を含む bundle をディレクトリごと移しても、内容が同じなら verify は通る。
- cheap な観測面も補い、[scripts/p1_manifest_inspect.py](D:/projects/llcore/scripts/p1_manifest_inspect.py) を追加した。これは 1 本の manifest と sibling `<manifest>.bundle.json` を読んで、entries 数・ordered file SHA・`combined.sha256`・`includes_base` を即時表示し、必要なら `--base-corpus-file` 付きで **その場で verify を再実行**する。prepare/probe が吐いた bundle を重い P1 train/eval 前に人間が cheap に監査するための導線として使う。その後の統合レビュー反映として、`main()` の fail-closed 出口も揃え、manifest 不在や壊れた bundle payload は traceback ではなく **`[verify] ...` + rc=1** に統一した。`includes_base=true` で base 未指定の skip 経路でも `files` / `bundle_sha256` の構造までは先に検証し、table と `[combined]` 行には `(unverified)` を付けて未検証表示だと明示する。さらに `tests/unit/test_p1_corpus_manifest_inspect.py` には repo root から **`sys.executable`（= pytest 実行中の解釈系）**で `scripts/p1_manifest_inspect.py` を起動する subprocess smoke も追加し、CLI entrypoint そのものが壊れていないことを固定した。`py -3.11` launcher / 3.11 pin 固有の解決はこの回帰の契約外である。最新局所検証は `py -3.11 -m pytest tests/unit/test_p1_corpus_manifest_inspect.py -q` = `10 passed`、`py -3.11 -m ruff check tests/unit/test_p1_corpus_manifest_inspect.py`。
- rerun 本線の観測性も少し上げた。`src/llcore/lm/corpus.py` の `verify_corpus_manifest_bundle()` は成功時に compact summary を返すようにし、`resolve_extra_corpus_files()` は希望時にその summary を収集できる。`llcore.lm train` / `eval` は sibling bundle 付き manifest を consume した場合、`[manifest] verified <name>: entries=... includes_base=... combined_sha256=... bundle_sha256=...` を先頭で 1 行表示するため、「どの束がどの hash で通ったか」を heavy rerun のログから直接追える。回帰は [tests/unit/test_lm_corpus.py](D:/projects/llcore/tests/unit/test_lm_corpus.py) に verify summary return を、[tests/unit/test_lm_cli.py](D:/projects/llcore/tests/unit/test_lm_cli.py) に probe/prepare が書いた bundle-backed manifest を `train` / `eval` が consume した際の verified line 出力を追加して固定した。最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `173 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py scripts/p1_manifest_inspect.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py scripts/p1_manifest_inspect.py tests/unit/test_lm_corpus.py tests/unit/test_lm_cli.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_prepare_aozora.py tests/unit/test_p1_corpus_manifest_inspect.py` が通過。
- 同じ provenance を stdout だけで終わらせず artifact にも落とした。`llcore.lm train` / `eval` の `verdict.json` は `manifest_verification` を持ち、verified/unverified の各 summary をそのまま残す。`train_state.pt` の `train_meta` にも同内容を保存するため、rerun 後にログが欠けても「どの manifest bundle を consume し、何が sidecar 不在だったか」を snapshot / verdict から再監査できる。**ただし verified は raw manifest entry が collapse せず、そのまま実消費集合へ入る場合に限る**。manifest に base 混入や重複 extra がある場合は、hash を付け替えて通すのではなく `collapse after base/duplicate filtering` で fail-closed reject する。回帰は [tests/unit/test_lm_cli.py](D:/projects/llcore/tests/unit/test_lm_cli.py) に unverified/verified 両経路の `verdict.json` / snapshot 記録を追加して固定し、最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `175 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py scripts/p1_manifest_inspect.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py scripts/p1_manifest_inspect.py tests/unit/test_lm_corpus.py tests/unit/test_lm_cli.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_prepare_aozora.py tests/unit/test_p1_corpus_manifest_inspect.py` が通過。
- `p1_manifest_inspect.py` の cheap 監査表示も train/eval の消費 semantics に寄せた。manifest の raw `entries`` と **effective_entries**（単一 manifest 内での dedup・base 除外後の実消費候補件数）を併記し、verify 成功時は `[effective] entries=... combined_sha256=... bundle_sha256=...` を追加する。**ただしこの `[effective]` hash を verified として出せるのは、raw entry が collapse しないときだけ**で、base 混入や duplicate 行で `entries != effective_entries` になった manifest は reject する。cheap inspection はあくまで単一 manifest の隔離ビューなので、`--extra-corpus-file` や複数 manifest をまたぐ cross-source dedup までは再現しない。回帰は [tests/unit/test_p1_corpus_manifest_inspect.py](D:/projects/llcore/tests/unit/test_p1_corpus_manifest_inspect.py) に raw 3 / effective 1 fixture の reject を追加して固定し、最新 gate は `py -3.11 -m pytest tests/unit/test_p1_corpus_manifest_inspect.py -q` = `11 passed`、report 用 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `176 passed, 401 deselected`。
- その follow-up として、`p1_manifest_inspect.py` の fail-closed edge も揃えた。`effective_entries=[]` かつ `includes_base=false` でも `_verified_manifest_summary()` と同じ分岐を使って `combined_sha256=None` / `bundle_sha256=sha256([])` を返すため、verify 済み bundle が `expected at least one corpus part` で誤って rc=1 になる経路は消えた。加えて `combined.chars` / `vocab_size` / `sha256` の構造も表示前に検証し、verify=failed の table / `[combined]` 表示にも unverified 印を付ける。cheap inspect の `effective_entries` は **単一 manifest 単位**の dedup/base 除外だけを反映し、`--extra-corpus-file` や複数 manifest をまたぐ cross-source dedup までは再現しない点も docstring に明記した。回帰は [tests/unit/test_p1_corpus_manifest_inspect.py](D:/projects/llcore/tests/unit/test_p1_corpus_manifest_inspect.py) に empty-effective / malformed-combined を追加し、最新 gate は `py -3.11 -m pytest tests/unit/test_p1_corpus_manifest_inspect.py -q` = `13 passed`、report 用 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `178 passed, 401 deselected`。
- さらに `p1_manifest_inspect --json` も rerun artifact と寄せた。JSON report は `manifest_verification` を持ち、**verified エントリだけ**は runtime `verdict.json` と同型になる。bundle 不在時の `unverified` は runtime 側と同じ語を使う一方、base 未指定の `skipped` と drift/shape 破損の `failed` は inspect 専用 status で、runtime `verdict.json` には現れない。したがって cheap inspection の JSON と rerun 後の `verdict.json` / `train_state.pt` は、**単一 manifest を単独で見るケースに限り**文字列整形なしで機械比較しやすい。`--extra-corpus-file` や複数 manifest 併用時の cross-source dedup は inspect が再現しないため、その場合の hash / entry_count divergence は仕様として残る。skip 経路でも `bundle.files[*]` の file-level shape を検証し、壊れた sibling bundle は rc≠0 で fail-closed にした。回帰は [tests/unit/test_p1_corpus_manifest_inspect.py](D:/projects/llcore/tests/unit/test_p1_corpus_manifest_inspect.py) に verified / skipped / unverified / malformed file shape / empty manifest JSON を追加し、最新 gate は `py -3.11 -m pytest tests/unit/test_p1_corpus_manifest_inspect.py -q` = `16 passed`、report 用 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `181 passed, 401 deselected`。
- cheap inspection を CI へつなぎやすくするため、`p1_manifest_inspect.py` に `--require-verified` も追加した。既定では `skipped` を rc=0 の cheap pre-check として残すが、この flag を付けると `verification.status != "passed"` はすべて rc=1 になる。つまり operator の手元確認では従来どおり軽く使え、CI/自動化では strict fail-closed に切り替えられる。回帰は [tests/unit/test_p1_corpus_manifest_inspect.py](D:/projects/llcore/tests/unit/test_p1_corpus_manifest_inspect.py) に skipped / bundle absent の strict reject を追加し、最新 gate は `py -3.11 -m pytest tests/unit/test_p1_corpus_manifest_inspect.py -q` = `18 passed`、report 用 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `183 passed, 401 deselected`。
- 2026-06-16 追記: 追加統合レビューに合わせて contract をさらに締め、**manifest に base path や duplicate extra が混入して `entries != effective_entries` になる場合は runtime / inspect とも `collapse after base/duplicate filtering` で reject** するようにした。これにより `verified` / `manifest_verification` が「検証していない effective hash」を名乗る経路は消えた。あわせて `p1_manifest_inspect.py` / `verify_corpus_manifest_bundle()` の shape 検証は `bool ⊂ int` を許さないよう補正し、`chars=true` / `vocab_size=false` の malformed bundle も fail-closed で拒否する。回帰は `tests/unit/test_lm_corpus.py` / `tests/unit/test_lm_cli.py` / `tests/unit/test_p1_corpus_manifest_inspect.py` に追加し、最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `184 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py scripts/p1_manifest_inspect.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py scripts/p1_manifest_inspect.py tests/unit/test_lm_corpus.py tests/unit/test_lm_cli.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_prepare_aozora.py tests/unit/test_p1_corpus_manifest_inspect.py` が通過。
- 2026-06-16 追記: provenance の最後の 1 手として [scripts/p1_manifest_reconcile.py](D:/projects/llcore/scripts/p1_manifest_reconcile.py) を追加した。これは **1 本以上の** `p1_manifest_inspect.py --json` report を argv 順に連結し、`--runtime` で明示指定した runtime の `verdict.json` または `train_state.pt` に残る `manifest_verification` と **そのまま機械比較**する CLI で、差分時は per-entry summary を出して rc=1 で止まる。比較は seed #32 に合わせて **content/contract フィールドだけ**で行い、`manifest_path` の絶対パス差は表示に残しつつ等価判定から外す。`.pt` 読み込みも `weights_only=True` + 整形エラー経路へ寄せ、壊れた checkpoint は生 traceback ではなく `[reconcile] ...` で rc=1 に統一した。`--json` で matched / mismatch を structured report 化でき、report payload 自体も `comparison_mode="positional"` を持つため、pre-run inspect JSON 群と post-run provenance の CI 比較も機械化しやすい。subprocess smoke も追加し、script entrypoint 自体を fixed した。さらに docstring / help を実装へ合わせて **multi-manifest は位置対応・順序依存比較**だと明記し、その matched / order-mismatch 回帰も追加した。ここでいう order-mismatch は **`COMPARABLE_FIELDS` が異なる entry を入れ替えた場合**に限る。同一内容で `manifest_path` だけが異なる entry の swap は、content-only 契約により意図的に検出しないことも回帰で固定した。未知 provenance フィールドは `COMPARABLE_FIELDS` に追加しない限り無視される点も docstring に残してある。回帰は [tests/unit/test_p1_manifest_reconcile.py](D:/projects/llcore/tests/unit/test_p1_manifest_reconcile.py) に path-only difference matched、JSON / checkpoint happy path、mismatch fail-closed、broken `.pt` 整形 reject、JSON report、subprocess entrypoint、multi-manifest matched / order-mismatch を追加し、report 用 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare or p1_manifest_reconcile" -q` = `204 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py scripts/p1_manifest_inspect.py scripts/p1_manifest_reconcile.py tests/unit/test_p1_manifest_reconcile.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py scripts/p1_manifest_inspect.py scripts/p1_manifest_reconcile.py tests/unit/test_lm_corpus.py tests/unit/test_lm_cli.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_prepare_aozora.py tests/unit/test_p1_corpus_manifest_inspect.py tests/unit/test_p1_manifest_reconcile.py` が通過。
- 2026-06-16 追記: その後 `reconcile` の false-positive 穴も塞いだ。`manifest_verification` entry は load 時に status ごとの required fields を fail-closed で検証し、`verified` では `entry_count / generated_by / includes_base / combined_sha256 / bundle_sha256`、degraded entry (`unverified` / `skipped` / `failed`) では `reason` を必須にした。これで inspect/runtime の **両側が同じ field を同時欠落**させた壊れ payload でも `_comparable()` が空一致して `"matched"` になる経路は消えている。回帰は [tests/unit/test_p1_manifest_reconcile.py](D:/projects/llcore/tests/unit/test_p1_manifest_reconcile.py) に「shared required field 両側欠落 reject」と「片側だけ型崩れ reject」を追加した。
- 2026-06-16 追記: `reconcile` の test は fixture 直書き一辺倒から少し離し、`p1_manifest_inspect.inspect_manifest()` が書く inspect JSON と `llcore.lm.corpus._verified_manifest_summary()` が作る runtime summary をそのまま突き合わせる happy-path 回帰を追加した。これは inspect 側の実配線 (`generated_by` / `combined.includes_base` / `effective_entries` / `base_file`) を通すための補強であり、**cross-producer drift を独立に検出するテストではない**ことも test docstring に明記している。
- 2026-06-16 追記: その後、`p1_corpus_probe.py` が実際に生成した manifest/bundle を `llcore.lm train` の runtime `verdict.json` と `reconcile` へ通す **actual producer/runtime happy-path** も 1 本追加した。これで handwritten fixture と shared helper だけに閉じた緑化は少し減った。
- 2026-06-16 追記: 続けて `p1_prepare_aozora.py -> llcore.lm train -> p1_manifest_reconcile.py` の **actual producer/runtime happy-path** も追加し、`includes_base=False` producer の disk round-trip を閉じた。ここでも summary hash 生成自体は shared helper 由来なので、主に検証しているのは **extras-only contract と manifest/bundle の on-disk handoff** であって、独立 hash 実装どうしの drift 検出ではない。
- 2026-06-16 追記: さらに `p1_corpus_probe.py` と `p1_prepare_aozora.py` がそれぞれ書いた 2 本の manifest/bundle を順序どおり `llcore.lm train` へ渡す **actual multi-manifest happy-path** も追加した。inspect 側も shipped CLI 契約へ寄せ、`p1_manifest_inspect.py --json` を **manifest ごとに 1 回ずつ**実行し、`p1_manifest_reconcile.py` が複数 inspect JSON を argv 順に positional 連結して runtime `manifest_verification` と照合する。したがってここで固定しているのは、主に **manifest ごとの on-disk inspect handoff**, **runtime handoff 上の manifest 群の順序保持**, **producer ごとの `includes_base` provenance 契約**である。順序入れ替え reject についても、synthetic reconcile fixture だけでなく **actual producer/runtime artifact + shipped inspect JSON 群**を使った order-mismatch 回帰を追加したが、これは **内容差のある entry**（今回の probe/prepare では `generated_by` / `includes_base` が異なる）を入れ替えた場合に mismatch へ落ちることを固定したものだ。`manifest_path` のみ異なる同一内容 entry の swap は、content-only 契約により意図的に検出しない。ただしこの「固定済み」は `reconcile` の **現 `COMPARABLE_FIELDS` 契約範囲**に限る。また `includes_base` 自体は per-entry provenance 表示であって、base の二重計上防止や corpus 合成保証そのものは runtime の dedup/resolve ロジックが担う。
- 2026-06-16 追記: resume 経路の provenance 欠落も塞いだ。`train_state.pt` の `train_meta` には **`requested_extra_corpus_files` と `extra_corpus_manifests`** も保存するようにし、manifest-backed run を `--resume-checkpoint` だけで再開しても、saved manifest 群から bundle 再検証をやり直し、`manifest_verification` が空配列へ潰れない。pre-fix snapshot 互換として、manifest path 群をまだ持たない旧 snapshot では saved `manifest_verification` をそのまま保持する fallback も残した。あわせて `_restore_training_snapshot()` と `_load_checkpoint()` は **`torch.load(..., weights_only=True)`** へ切り替え、resume/eval の CLI 入力から full pickle を実行しないようにした。回帰は [tests/unit/test_lm_cli.py](D:/projects/llcore/tests/unit/test_lm_cli.py) に「manifest 再指定なし resume でも unverified summary を再 emit」「legacy snapshot でも saved summary を維持」を追加し、report 用 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare or p1_manifest_reconcile" -q` = `206 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/__main__.py tests/unit/test_lm_cli.py`、`py -3.11 -m ruff check src/llcore/lm/__main__.py tests/unit/test_lm_cli.py` が通過した。
- 2026-06-16 追記: その軽微 follow-up も片付けた。`verdict.json` には **full training/eval input の `corpus_sha256`** を保存するようにし、prepare 由来 manifest の `combined_sha256`（extras-only provenance）を「学習コーパス全体の指紋」と誤読しにくくした。さらに `iter_num>=max_iters` の完了 snapshot を `--resume-checkpoint` で読む場合も、成功終了前に **`verdict.json` / `model.pt` / `tokenizer.json` / `model_viz.json` を再 emit** するため、「artifact を消した completed run を resume すると exit 0 なのに成果物ゼロ」という状態を避けられる。manifest collapse のエラー文言も、「manifest か CLI extras のどちらに重複/base 混入があるか」を誤読しにくい表現へ補正した。最新 report 用 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare or p1_manifest_reconcile" -q` = `207 passed, 401 deselected`。
- 回帰は [tests/unit/test_lm_corpus.py](D:/projects/llcore/tests/unit/test_lm_corpus.py) に stale absolute path 無視を、[tests/unit/test_lm_cli.py](D:/projects/llcore/tests/unit/test_lm_cli.py) に **prepare が書いた manifest+bundle をそのまま train/eval が consume できる round-trip** を追加して固定した。最新 gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare" -q` = `163 passed, 401 deselected`、`py -3.11 -m mypy src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py`、`py -3.11 -m ruff check src/llcore/lm/ scripts/p1_corpus_probe.py scripts/p1_prepare_aozora.py tests/unit/test_lm_corpus.py tests/unit/test_lm_cli.py tests/unit/test_p1_corpus_probe.py tests/unit/test_p1_prepare_aozora.py` が通過。
- `D:\tools\raptor` 側の現 dirty は `_bazue_*` 3 件削除のみで、self_evolving_agents rerun 束には混ぜない
- 現在地点は「`self_evolving_agents` の (b) rerun 方針は承認済み、Anthropic 要約器の疎通確認も通過済み、残る主ブロッカーは rerun 本実行の人間判断待ち」という状態。最小 fetch 検証と query 汚染是正までは完了済み
- 現在の llcore 側 `git status` では、**staged は `scripts/p1_compare.py` と `tests/unit/test_p1_compare.py` の 2 件のみ**。unstaged/未追跡には `docs/ARTICLE_SEEDS.md` / `docs/LM_P0_PLAN.md` / `docs/LM_RECURRENT_PLAN.md` / `docs/PROGRESS.md` / `docs/SESSION_SUMMARY.md` / `docs/next_plan.md`、`src/llcore/lm/__main__.py` / `data.py` / `trainer.py`、新規 `src/llcore/lm/corpus.py`、新規 `scripts/p1_*` 群、新規 `tests/unit/test_lm_cli.py` / `test_lm_corpus.py` / `test_p1_*` 群が残っている。`docs/SESSION_SUMMARY.md` は Stop hook `raptor-auto-summary` が stop 時点で自動生成した point-in-time snapshot であり、その後の staging 状態とはズレうる。fullsense 側も `collect_research_seeds.py` など別 repo の dirty が残っているため、実体は各 repo の `git status` を正とする
- 次は `verified_safe_learning` / `self_evolving_agents` の人間判断待ちを処理するか、人間判断なしで進めるなら `(b)` rerun 本実行の判断回収と準備再開へ進む
- 再開時は `next_plan.md` の「今回追加で進めた内容」と「次の具体的な一手」から続行
- 2026-06-16 追記: Kaggle offload の local prep として [scripts/build_kaggle_lm_compare_bundle.py](D:/projects/llcore/scripts/build_kaggle_lm_compare_bundle.py) を追加した。`llcore.lm.compare` 用に `--corpus-file` 必須の deterministic bundle を生成し、`input_corpus.txt` / `config.json` / `kernel-metadata.json` / `runner.py` / `README.md` / `src/llcore/` snapshot / `bundle_manifest.json` を 1 ディレクトリへまとめる。`__pycache__` / `*.pyc` は copy しない。
- 2026-06-16 追記: builder はその後 safe-default 化した。`bundle_dir` が repo root / `src/` tree / source snapshot target と重なる場合は fail-closed rejectし、**既存の非 bundle ディレクトリ**も削除せず reject する。既定 metadata は **private + internet disabled + GPU disabled**。bundle 再生成時は認識済み bundle に限って対象ディレクトリ全体を clean に作り直すため、stale file を引きずらない。GPU / internet / public は CLI opt-in に落とし、`machine_shape` は `--enable-gpu` 時のみ出力する。
- 2026-06-16 追記: focused 回帰 [tests/unit/test_build_kaggle_lm_compare_bundle.py](D:/projects/llcore/tests/unit/test_build_kaggle_lm_compare_bundle.py) は `2 passed`。smoke 生成 `out/kaggle_lm_compare_smoke` でも bundle layout を確認済みで、外部 publish は未実施。`kaggle kernels push -p ...` は引き続き人間ゲート。
- 2026-06-16 追記: builder 追加後の broad gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare or p1_manifest_reconcile or memory_footprint or kaggle_lm_compare_bundle" -q` で `214 passed, 401 deselected`。static checks は `py -3.11 -m mypy scripts/build_kaggle_lm_compare_bundle.py tests/unit/test_build_kaggle_lm_compare_bundle.py` と `py -3.11 -m ruff check scripts/build_kaggle_lm_compare_bundle.py tests/unit/test_build_kaggle_lm_compare_bundle.py` が通過。
- 2026-06-16 追記: builder の focused 回帰はその後 `5 passed` へ拡張し、generated `runner.py` を tiny corpus / tiny config で **ローカル subprocess 実行**して `artifacts/lm_compare.json` まで吐けることを固定した。README には「`machine_shape` を含む GPU metadata は push 前に live Kaggle schema/CLI で確認する」旨と、**`torch` は Kaggle 既設環境に依存しバージョン pin はしていない**旨も追加済み。
- 2026-06-16 追記: push 前の local gate として [scripts/kaggle_bundle_preflight.py](D:/projects/llcore/scripts/kaggle_bundle_preflight.py) も追加した。これは bundle layout / metadata / config / manifest の **shape だけでなく整合性・完全性**も fail-closed に検証する。具体的には `input_corpus.txt` の sha256 再計算、`config.corpus_sha256 == manifest.corpus_sha256`、bundled `src/llcore` snapshot の **`source_sha256` 再計算照合**、`runner.py` と `config.json` の **sha256 再計算照合**、`kernel-metadata.json.id == bundle_manifest.json.kernel_id`、`kernel-metadata.json.code_file == "runner.py"`、`kernel-metadata.json.title == bundle_manifest.json.title`、`is_private=true` / `enable_internet=false` の **safe-default 維持**と manifest との一致、`enable_gpu` / `machine_shape` の整合、`copied_files` の **必須キー集合**と bundle 外 escape reject + 実在確認、`CompareConfig(**compare_config)` の round-trip まで見る。必要なら `--run-runner` で bundled `runner.py` をローカル実行し、stale `artifacts/lm_compare.json` を事前削除したうえで再生成と JSON parse、さらに sidecar の `artifacts/lm_compare.md` / `.svg` 生成まで確認する。`PYTHONDONTWRITEBYTECODE=1` で `.pyc` 汚染も抑え、再実行時の false drift を避ける。`--runner-timeout`（既定 300 秒）も追加し、timeout / `OSError` は traceback ではなく clean に rc=2 へ落とす。Kaggle への通信は一切行わず、外部 publish 前の最後の cheap check だけを担う。
- 2026-06-16 追記: Kaggle local prep + preflight の focused 回帰は `tests/unit/test_build_kaggle_lm_compare_bundle.py` と `tests/unit/test_kaggle_bundle_preflight.py` が合わせて `20 passed`。`input_corpus.txt` 改ざん reject、manifest `kernel_id` 不一致 reject、壊れた `compare_config` reject、`code_file` 不一致 reject、`copied_files` escape reject、GPU/machine-shape 不整合 reject、safe-default drift reject、timeout の clean rc=2、runner sidecar 生成まで固定している。builder/preflight 用の `mypy` / `ruff` も通過済み。最新 broad gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare or p1_manifest_reconcile or memory_footprint or kaggle_lm_compare_bundle or kaggle_bundle_preflight" -q` で `232 passed, 401 deselected`。
- 2026-06-16 追記: その後さらに fail-closed を揃え、builder は **empty existing dir を非-bundle として reject**し、`CompareConfig(...)` の CLI 入力エラーも raw traceback ではなく `error:` + `rc=2` へ落とすようにした。`memory_footprint_harness.py` も bad `--lengths` / negative `--warmup` を同じく clean `rc=2` へ統一した。最新 broad gate は同コマンドで `234 passed, 401 deselected`。
- 2026-06-16 追記: さらに [scripts/prepare_kaggle_lm_compare_bundle.py](D:/projects/llcore/scripts/prepare_kaggle_lm_compare_bundle.py) を追加し、**build → preflight → 次に人間が打つ `kaggle kernels push -p ...` の提示**までを 1 コマンドで実行できるようにした。publish 自体は依然しない。focused 回帰は builder / preflight / memory harness / prepare wrapper を合わせて `39 passed`。最新 broad gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare or p1_manifest_reconcile or memory_footprint or kaggle_lm_compare_bundle or kaggle_bundle_preflight or prepare_kaggle_lm_compare_bundle" -q` で `246 passed, 401 deselected`。
- 2026-06-16 追記: 追加 hardening として、`prepare_kaggle_lm_compare_bundle.py` の `push_command` は **PowerShell でそのまま貼れるよう bundle path をダブルクオート**するようにした。`--runner-timeout` は prepare / preflight の双方で既定 `300` 秒・`<1` reject に揃え、`--run-runner` は heavier config ではより長い timeout が要る可能性を `[note]` で明示する。builder / preflight / prepare の 3 スクリプトは `src/` を自前で `sys.path.insert` し、editable install 前提を外した。builder の bundle 再生成も `rmtree -> replace` ではなく **backup rename -> replace -> success後cleanup / 失敗時restore** に変更し、repo 内出力は fail-closed reject に寄せた。
- 2026-06-16 追記: その次の gate として [scripts/kaggle_push_readiness.py](D:/projects/llcore/scripts/kaggle_push_readiness.py) も追加した。これは **既存 bundle に対して** preflight を再実行し、さらに push credential (`kaggle.json` または `KAGGLE_USERNAME`+`KAGGLE_KEY`) の存在と `kaggle kernels list -m --page-size 1 --csv` の軽量疎通、必要時のみ `kaggle quota -v` を使って **limited live Kaggle（auth/quota のみ）** の readiness を確認する wrapper である。publish は依然行わず、最後に quoted `kaggle kernels push -p ...` を表示するだけ。focused 回帰は builder / preflight / prepare / readiness を合わせて **`41 passed`**（この 4 ファイル集合）、最新 broad gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare or p1_manifest_reconcile or memory_footprint or kaggle_lm_compare_bundle or kaggle_bundle_preflight or prepare_kaggle_lm_compare_bundle or kaggle_push_readiness" -q` で **`253 passed, 401 deselected`**。
- 2026-06-16 追記: その後 live smoke も 1 度実施した。fresh rebuild 後の正本 bundle は `C:\Users\puruy\AppData\Local\Temp\llcore_kaggle_livecheck` で、manifest hash は `corpus_sha256=0ba240aece8561da15f9f502a1cc8cd367a64e3818a2e229eb8dc42ddc348e24`、`source_sha256=6073f935a647d315299b99fc00b010bc962e69498e90a6d545e7489787e038ac`、`runner_sha256=094336da508dcfb4899e98e7a0cb37b4cccd32dcb396d5d82e609b660ba05f2d`、`config_sha256=ab47ff86eef9f6c42d4ddc119853facaf5b3e1b7b5a3cb1c1a2982b63fff3f80`。この bundle に対する `kaggle_push_readiness.py` は local preflight を通過し、live 側は **auth 未ログイン**で `error: kaggle auth check failed: You must log in to Kaggle...` を返して clean **`rc=3`** で停止した。つまり **確認済みなのは auth failure path のみ**で、quota 取得までは未到達である。現在の exit code は `rc=2`=local validation / preflight failure、`rc=3`=auth failure、`rc=4`=quota failure で分離している。なお旧 `out/kaggle_lm_compare_smoke` は `source_sha256` 追加前の古い manifest 世代なので、current preflight では通さず、fresh rebuild 後の bundle を正本とする。
- 2026-06-17 追記: Kaggle gate の false negative も追加で解消した。`kaggle_bundle_preflight.py` は builder が記録した `kernel-metadata.json` と manifest の **宣言一致**（`title` / `is_private` / `enable_internet` / `enable_gpu` / `machine_shape`）を検証し、safe-default 自体は **builder の既定値**（private + internet disabled + GPU disabled）で担保する形へ寄せた。したがって `--public` / `--enable-internet` の opt-in bundle も、manifest と metadata が一致していれば preflight を通る。一方 `--run-runner` は now truly gates all three sidecars: stale `artifacts/lm_compare.json` / `.md` / `.svg` を事前削除し、runner 後は 3 ファイルの再生成まで assert する。
- 2026-06-17 追記: `kaggle_push_readiness.py` の quota 判定は bundle metadata を参照するよう改めた。CPU bundle (`enable_gpu=false`) は **quota check 自体を skip** し、GPU bundle (`enable_gpu=true`) のみ **GPU-like row**（resource/name/quota/type の `"gpu"` substring match）の残量を判定する。CSV は `remaining` 列があればそれを優先し、無ければ `used/total` を単位付き文字列（例 `30.00h`）から `float` で解釈して再計算する。auth は OAuth token ではなく **push credential + `kaggle kernels list -m --page-size 1 --csv` 疎通**で見る。exit code は引き続き `rc=2`=local preflight、`rc=3`=auth、`rc=4`=quota で分離。focused 回帰は builder / preflight / prepare / readiness の 4 ファイル集合で **`46 passed`**、最新 broad gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare or p1_manifest_reconcile or memory_footprint or kaggle_lm_compare_bundle or kaggle_bundle_preflight or prepare_kaggle_lm_compare_bundle or kaggle_push_readiness" -q` で **`258 passed, 401 deselected`**。
- 2026-06-17 追記: その後さらに Kaggle readiness の false-negative 穴を閉じた。`kaggle_bundle_preflight.py` は `is_private` / `enable_internet` / `enable_gpu` / `enable_tpu` を **文字列 `"true"` / `"false"` のみ許容**する fail-closed 検証にし、`kernel_id` / `title` も非空・builder では `owner/slug` 形式まで reject する。`kaggle_push_readiness.py` の quota 判定は `remaining` 列優先・無ければ `used/total` 再計算という方針を維持しつつ、CPU bundle はアクセラレータ quota 不要、GPU bundle は **GPU row のみ**で判定する。focused 回帰は builder / preflight / prepare / readiness の 4 ファイル集合で **`50 passed`**、最新 broad gate は `py -3.11 -m pytest tests/unit -k "lm or corpus or probe or p1_compare or p1_prepare or p1_manifest_reconcile or memory_footprint or kaggle_lm_compare_bundle or kaggle_bundle_preflight or prepare_kaggle_lm_compare_bundle or kaggle_push_readiness" -q` で **`262 passed, 401 deselected`**。なお live 実測は依然 **auth failure path のみ**で、quota live path 自体は未確認のまま。
- 2026-06-17 追記: その後さらに現物 focused gate を再実行し、`tests/unit/test_memory_footprint_harness.py` を含む 5 test ファイル集合で **`57 passed`**、対象 10 ファイルの `mypy` / `ruff` も通過した。repo 外 fresh bundle の **handoff 正本は non-Temp `D:\projects\llcore_kaggle_livecheck_20260617c`** とし、この candidate に対する現行 live smoke では `kaggle_push_readiness.py` が **CPU ready path `rc=0`** に到達し、`quota_rows=0 quota_checked=cpu` を表示した。したがって現行契約で live 確認済みなのは **CPU bundle の ready path** までであり、GPU quota live path と actual `kaggle kernels push` は引き続き未実施。push handoff の SSOT は `docs/next_plan.md`。
- 2026-06-17 追記: 追加で GPU bundle `C:\Users\puruy\AppData\Local\Temp\llcore_kaggle_gpu_livecheck_20260617` でも live smoke を試した。bundle 生成自体は通るが、このマシンの **Kaggle CLI 2.2.1** では `kaggle quota` / `kaggle quota -v` 自体が `not enough values to unpack (expected 2, got 1)` で失敗するため、`kaggle_push_readiness.py` は **`rc=4`** で止まった。これは現行 parser や row-match 条件以前の **local CLI failure** であり、現時点の GPU quota live path は「未実施」ではなく **実施したが CLI 側不具合で確認不能** と整理するのが正確。
- 2026-06-17 追記: TPU opt-in だけは quota gate の対象外なので、`kaggle_bundle_preflight.py` は `enable_tpu` の metadata↔manifest 整合までは見るが、`kaggle_push_readiness.py` は **`enable_tpu=true` の bundle を未対応として clean `rc=2` で reject**する。これは silent bypass より fail-closed を優先した措置で、focused の `tests/unit/test_kaggle_push_readiness.py` は **`9 passed`**。
