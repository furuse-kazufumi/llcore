# [HISTORICAL / superseded 2026-06-22]
#
# This file was the incremental allowlist of strict-clean scripts while scripts/ was
# being migrated to `mypy --strict` one file at a time. As of 2026-06-22 ALL of scripts/
# (top-level + subdirectories) is strict-clean, so the CI gate (.github/workflows/ci.yml)
# now runs `mypy src scripts --strict` directly — no allowlist needed. A newly added
# script must pass --strict or CI fails.
#
# Kept (not deleted) for provenance. If you ever need to temporarily exempt a script,
# prefer fixing it; this file is no longer read by CI.
